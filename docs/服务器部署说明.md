# 原型服务器部署

部署日期：2026-09-17。部署当前 prototype 及 SQLite 一致性快照。

- 前台：http://89.45.45.158:5190/
- 后台：http://89.45.45.158:5190/#/admin/overview
- 程序：`/opt/onemap/releases/20260917`，当前链接 `/opt/onemap/current`
- 数据库：`/opt/onemap/data/demo.sqlite3`
- Python 环境：`/opt/onemap/venv`
- 服务：`onemap.service`，以独立 onemap 用户运行，开机自启，异常自动重启。
- 配置：`/etc/systemd/system/onemap.service`

维护命令（服务器执行）：

```sh
systemctl status onemap
systemctl restart onemap
journalctl -u onemap -n 100 --no-pager
```

现有 80/443 站点保留。此次直接使用 5190 端口，暂未增加 Nginx 代理或缓存，后续另行配置。更新程序时保留独立数据目录，并在数据库迁移前备份。

## 2026-09-17 初始化接口性能修复

`server.py` 已开启 JSON 按需 gzip 压缩，并将 bootstrap 数据组装及网络发送移出全局状态锁。读取数据库时仍持锁获取独立快照；保留 `Cache-Control: no-store`，添加 `Vary: Accept-Encoding`，未启用跨用户接口缓存。

公网实测（单次修复前、修复后各身份三次，受网络波动影响）：

| 身份 | 修复前传输大小 | 修复后传输大小 | 修复前总耗时 | 修复后总耗时 |
| --- | --- | --- | --- | --- |
| 普通用户 | 146,044 B | 22,465 B | 1.77 s | 0.97–1.03 s |
| 管理员 | 354,355 B | 38,664–38,665 B | 2.05 s | 1.03–1.05 s |

服务器本机原始生成耗时约 43–54 ms，公网首字节仍约 0.66–0.71 s，后续需结合网络线路及前端重复初始化请求继续优化。压缩需客户端声明 `Accept-Encoding: gzip`，浏览器自动支持。

验证：新增 `prototype/test/test_bootstrap_http.py` 3 项 HTTP 回归测试，门户管理 27 项测试通过。上线前服务器文件备份：`/opt/onemap/server-before-bootstrap-fix.py`。

复查服务日志发现初次打包时误排除了 `assets/nmg-demo/data`。现已补齐整个地图静态数据目录，`runtime.json` 返回 HTTP 200，并核对运行文件清单无缺失。

## 菜单切换初始化请求优化

前端初始化数据改为页面内存缓存，按用户和前后台模式区分，有效期 30 秒，同时发生的请求合并。路由切换及窗口聚焦在有效期内复用数据；手动刷新、保存后的刷新保留强制请求。业务操作使缓存失效，地图读取和浏览埋点不使缓存失效；身份切换清空缓存。缓存不写入 localStorage/sessionStorage，也不共享到 Nginx。

缓存过期后，在下一次导航或聚焦时获取最新数据；其他用户的修改可能最多延后约 30 秒在这些刷新触发点体现，手动刷新可立即拉取。

验证：4 项 Node 缓存测试通过；公网浏览器连续切换前台栏目及后台菜单均新增 0 次 bootstrap 请求，手动刷新新增 1 次，无脚本错误。更新文件为 app.js、index.html、frontend/bootstrap-cache.js。更新前前端文件备份在 `/opt/onemap/backups/bootstrap-cache/`。

## 地图对象属性查看

已独立启用一张图成果场景的点选属性能力，修复原场景未配置 query 组件导致点选无效的问题。点击已显示的授权业务要素时，高亮并在右侧顶部展示对象属性；隐藏的业务面板会自动展开，手机端滚动至属性面板。展示通用字段及图层专属字段，保留零值/否值，排除几何数组等内部结构，提供关闭按钮；原有分析授权保持不变。

验证：属性字段测试 2 项通过；本地及公网地图直接点击、台账定位均能打开属性面板，公网页面无脚本错误。更新前文件备份：`/opt/onemap/backups/before-map-properties.tar.gz`。
