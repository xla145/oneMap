# 一张图成果地图重设计与参考数据提取

日期：2026-09-16。依据《一张图成果地图业务重设计PRD》，并按用户补充要求直接提取 `bak/内蒙古一张图demo` 的地图与数据。

## 查看入口

- 新版工作台：`http://127.0.0.1:5190/#/admin/integration-results`
- 提取的完整原版地图：`http://127.0.0.1:5190/assets/nmg-demo/index.html`
- 数据提取目录：`prototype/assets/nmg-demo/`，结构及重建命令见目录下 README。

新版仍使用项目既有OpenLayers，复用原版的天地图配置、行政边界和原始业务数据；提供“原版地图”独立入口保留原Leaflet页面作对照。

## 已完成

1. 深绿地图工作台，顶部业务导航、可收起目录、地图查询工具、右侧区域/对象面板及底部台账/历史/方案。后台导航收窄以增加地图空间，悬停或键盘聚焦可展开。
2. 默认地图接入28层、1018个原业务要素及158个独立乡镇点位。按真实几何类型显示点线面，图层颜色与原demo一致；提供搜索、主题分组、透明度、定位与空间查询。
3. 自治区/盟市/旗县定位；区域统计读取原demo数据，当前地图对象计数按所选行政区属性筛选。两种统计口径明确分离，拖动地图不改变业务统计范围。
4. 八主题专题入口，按主题关联demo图层；四大业务模块的40项功能可查看业务定义、流程和成果，点击可同步关联图层。原文政策与建设状态不被解释为本项目已完成状态。
5. 选中面要素可直接带入地块核查；同一主地图承载追加绘制、批量校验、异步进度、结果定位及CSV/HTML/GeoJSON导出。继续使用原有三项示例核查规则，未把参考demo边界认定为正式审批数据。
6. 展示原demo的12项2021—2025年时序指标、曲线和明细，可导出CSV。无历史影像和逐年矢量，因此未伪造卷帘或空间变化计算。
7. 工具、应用、成果监测继续读取既有中心数据；参考demo不会注册为正式资源，也不会计入已发布业务资源数量。
8. 地图方案、查询历史、对象台账、PNG地图导出及原有权限校验保留。参考运行接口要求成果查看权限，参考素材本身作为公开演示资产提供。

## 数据处理

原始JS、完整HTML和GeoJSON逐文件保留；纯JSON从原定义提取。运行数据保留所有原始属性，对标为GCJ-02的要素做近似坐标转换，与已有地图边界处理一致，不能用于测绘。原始业务面积与地图几何面积分别理解。1018个原业务要素不包含独立的158个乡镇点位；工作台补齐乡镇目录，运行对象总数1176。

提取包另外保存原地形行政区图片、专题POI和模块定义。单文件原版地图可独立打开；新版业务界面使用原业务图层及区域统计，未将全部POI再重复叠加为第二套同类对象。

## 验证

- Python全量248项通过，包括新增参考数据数量、点线面空间查询、权限拒绝、图层注入拒绝及收藏恢复验证。
- JavaScript工作区测试和修改文件语法检查通过。
- 独立数据库浏览器验证通过：28层加载、盟市统计联动、对象带入核查、真实异步结果、八主题/业务模板、12项时序、工具/应用/监测/收藏导航、390px布局及无浏览器异常。
- 截图：`prototype/screenshots/results-map-desktop.png`、`results-map-analysis.png`、`results-map-mobile.png`。

正式三维分析、历史影像卷帘、完整八项正式规则和业务系统办理状态仍需真实服务接入；当前页面明确显示具体数据来源及缺口。
