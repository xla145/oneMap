# 一张图成果第二轮补充说明

日期：2026-09-16。承接需求对比中可在现有原型内完成的缺口。

## 已补充

| 项目 | 当前行为 | 边界 |
| --- | --- | --- |
| 图库组合 | 全部可见图库中的已授权图层可叠加到当前地图，不必切换基础场景；同一资源去重，可单独关闭、调整透明度、定位或移除叠加 | 数据取自已有可访问的已发布二维场景，不接受客户端上传的图斑作为查询依据；未挂载场景的资源仍不能绘制 |
| 组合查询与收藏 | 范围查询包含组合图层；组合来源随查询历史、个人收藏及会话状态保存；刷新、收藏恢复时重新校验 | 来源下架、撤权或移除时不恢复对应图层；查询不能绕过授权 |
| 多地块编辑 | 地图绘制追加地块；列表支持单块重绘、名称和坐标编辑、删除；保留其他地块及其标识 | 最多100个地块；坐标、几何与面积继续由服务端校验；非经纬度输入追加绘制前先校验转换 |
| 结果一致性 | 改变地块草稿清理旧结果；排队和运行期间锁定输入，可取消任务 | 避免把旧核查结果误认为修改后的地块结论 |
| 资源规模 | 同屏增加可见数据库表、矢量图层、栅格图层、证照图层、形态未登记数量；保留登记容量及覆盖数量 | 数据表按原目录对象计数；栅格与证照是图层数量，不能替代文件数、证照份数或实时容量 |
| 统计人数 | 查询、成功调用、入口访问各按所选活动去重统计人数 | 仅打开入口的用户不再计入成功调用人数；累计和本月按所选对象统计，不受日期和调用部门筛选影响 |
| 移动布局 | 统计和目录筛选表单改为可读的两列布局，长对象选择占满一行 | 修复原先虽未溢出但字段被挤窄、标签竖排的问题 |

## 验证

- Python 全量测试242项通过（包含工作区已有其他模块测试）；新增组合图层去重、查询、收藏、来源失效、未挂载资源拒绝、统计人数及规模口径测试。
- 地块编辑脚本验证已有地块保留、ID唯一、重绘保留标识、数量上限与无效索引。
- 浏览器验证跨场景叠加、不切换基础场景、刷新恢复、移除及收藏恢复、追加绘制、单块编辑重绘删除、重新核查、规模卡片和390px布局。
- 既有地图浏览器回归通过：查询、历史、迟到响应隔离、切换恢复、移动布局和撤权清理。
- 写入验证使用 `/tmp/onemap-results-round2-qa.sqlite3`；额外测试场景仅存在隔离库，不向现用库补入演示场景。

## 仍需后续工作

本轮未接入新的真实图层、三维引擎、时序数据、八类正式专题或外部业务系统。未挂载场景的目录资源仍需先配置数据来源；没有将资源目录记录转换成虚构业务数据。真实容量、栅格文件量、证照份数及接入进度需来源系统接口支持。主题、手册和发布信息仍需业务方提供，现用库没有自动填充或猜测。

## 现用服务更新

- 更新前备份：`/tmp/onemap-before-results-round2-20260916-183540.sqlite3`。
- 原业务字段、分析输入及任务核对保留；全状态比对仅发现一条运行告警在服务恢复后自动变为“已恢复”，未回滚该正常状态变化。
- 切换记录：`/tmp/onemap-results-round2-migration.json`。
