# 智能中心 Demo 验证记录

验证日期：2026-09-14。验证对象为本地 `prototype/` 工程，入口端口 5190。

## 自动验证

| 检查 | 结果 |
| --- | --- |
| Python 领域测试 | 10 项通过 |
| JavaScript 语法检查 | 通过 |
| Python 编译检查 | 通过 |
| 浏览器页面 / 视口检查 | 24 项通过 |
| 浏览器错误记录 | 未发现错误 |

浏览器检查覆盖前台 6 个主入口和后台 10 个主入口，1440px 全量检查、390px 六个代表页面检查、1920px 前后台首页检查。各页面均已加载、有正文，无页面整体横向溢出和损坏图片。后台宽表在自身容器内横向滚动。

可复查的结构化结果：[browser-checks.json](browser-checks.json)。可重跑脚本：`python3 prototype/check_browser.py`，需要安装 `agent-browser` 并启动服务。

## 实际交互验证

| 流程 | 实際验证内容 | 结果 |
| --- | --- | --- |
| 资源检索与元数据 | 后台为巡查库表增加“风险巡访”同义词，保存并发布；前台新会话查询 | 命中已发布资源，可打开元数据详情 |
| 跨资源申请审批 | 前台选择库表和工具提交一张申请；另一个浏览器中的后台通过库表、驳回工具 | 前台刷新显示“部分通过”；获批库表变为已授权，工具仍需申请 |
| 知识引用 | 后台修改知识标签并发布新版本；前台政策助手提问并打开引用 | 引用显示 v2.0 与对应切片，原文可读 |
| 指标口径 | 后台修改示例值与口径，试算并发布；前台指标助手查询 | 两端均显示 400 公顷、三个演示值及新版本统计口径 |
| 反馈改进 | 前台资源回答提交反馈；后台转为待审样例、修正标准答案并发布 | 前台相同问题采用修正后的答案 |
| 用户记忆 | 后台把林晓默认区域改为呼和浩特市、回答改为详细；前台开始新会话 | 显示新默认区域，回答包含详细说明；明确区域覆盖偏好有领域测试覆盖 |
| 字段编辑 | 在元数据编辑器点击添加字段 | 字段从 3 行增至 4 行，编辑器保持打开，不触发意外提交 |
| 刷新与重启持久化 | 实际前后台刷新；重启本地服务后重新访问 | 申请、授权、版本、对话及偏好均保留 |

验证中修复了区域条件优先级、反馈样例参与匹配、表单辅助按钮意外提交、长编辑器滚动及标题布局等问题。

## 截图

- [桌面前台工作台](screenshots/front-home-1440.png)
- [桌面后台总览](screenshots/admin-overview-1440.png)
- [窄屏前台工作台](screenshots/front-home-390.png)
- [窄屏后台资源管理](screenshots/admin-resources-390.png)

## 验证边界

本次验证针对交互式原型及其本地领域逻辑，不代表生产系统验收。真实模型效果、生产身份认证、真实审批对接、GIS 服务及多用户压力测试不在本轮范围内。

实际交互验证生成的申请、会话和修正样例保留在本机，方便继续体验；后台“基础管理 → 演示设置”可确认后恢复初始数据。

## 2026-09-14 核查整改版本验证

依据 `docs/智能中心原型修改方案.md` 补齐本地可执行能力后，本轮重新验证：

- `python3 -m unittest discover -s prototype -p 'test_*.py'`：32 项通过。
- `node --check prototype/app.js` 与 Python 编译检查通过。
- `DEMO_QA_URL=http://127.0.0.1:5193 python3 prototype/check_capabilities_browser.py`：17 项交互/移动视口检查通过。
- `DEMO_QA_URL=http://127.0.0.1:5193 python3 prototype/check_browser.py`：24 项页面/视口检查通过，无浏览器错误、页面空白或整体横向溢出。
- 浏览器测试使用独立数据库 `/tmp/onemap-capability-final.sqlite3`；截图为 `screenshots/v2-*.png`，结果为 `capability-browser-checks.json`、`capability-layout-checks.json`。
- 查看知识图谱、协同回答和指标趋势截图，网络连接、图表、弹窗及文字显示正常。
- 原 5190 服务已备份数据库后重启；校验已有申请、授权、会话、反馈和用户完整保留，主要资产历史与原有字段未覆盖。

新增测试重点验证：字段查询与显示开关、字典值与模板、图谱端点、长文和混合结果批次、提示词发布隔离、步骤实际执行与循环保护、动态指标与复合依赖、区域行级限制、只读 SQL/API、评测不泄漏标准答案、任务接力与跨用户隔离、反馈改进以及接口追踪。

本次测试证明上述本地流程可运行；真实模型、生产接口和完整业务领域指标库不在本轮验证范围。完整变化与边界见 `docs/智能中心修改完成说明.md`。

## 2026-09-15 前台门户改版验证

- 前台改用顶部导航和门户首页，助手为独立工具使用页，后台保留管理布局。
- `node --check prototype/app.js` 通过。
- `DEMO_QA_URL=http://127.0.0.1:5194 python3 prototype/check_portal_browser.py`：36 项检查通过，无浏览器错误或页面整体横向溢出。
- 检查包括首页分类筛选、资源详情、知识正文、五类服务、场景提问、门户搜索、个人申请与偏好、前后台切换，以及 8 个前台页面在 1440/1024/390 宽度的布局和手机折叠菜单。
- 截图复核：门户首页、工具服务和助手页面；文件为 `screenshots/portal-*.png`。
- 所有提问与会话写入使用独立数据库 `/tmp/onemap-portal-qa-20260915.sqlite3`，没有写入原演示数据库。业务服务代码未修改，本轮重点验证前台导航与交互。
- 静态资源增加门户版本标识，使用新版首页链接可完整加载新布局。
